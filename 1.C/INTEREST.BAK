#include<stdio.h>
#include<conio.h>

void main()
{
	int p,r,n;
	int total=0;

	clrscr();

	printf("enter the amount:");
	scanf("%d",&p);

	printf("enter the rate:");
	scanf("%d",&r);

	printf("enter the time:");
	scanf("%d",&n);

	total=p*r*n/100;
	printf("area of interest is:%d",total);
}