#include<stdio.h>
#include<conio.h>
void main()
{
    int unit;
    float total;
    float charge;

    clrscr();

    printf("enter the unit:");
    scanf("%d",&unit);

    if(unit<=100){
    total=unit*0.60+50;
    printf("unit is %.2f",total);
    }
    else if(unit>100&&unit<200){
    total=unit*0.80+50;
    printf("unit is %.2f",total);
    }
    else if(unit>200&&unit<300){
    total=unit*0.90+50;
    printf("unit is %.2f",total);
    }
    else if(unit<300){
    charge=unit*15/100;
    }
}
